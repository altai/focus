export NOVA_API_KEY="{{ key }}"
export NOVA_USERNAME="{{ user }}"
export NOVA_PROJECT_ID="{{ tenant }}"
export NOVA_URL="{{ keystone_url }}"
export NOVA_VERSION="1.1"
export OS_AUTH_KEY="{{ key }}"
export OS_AUTH_STRATEGY=keystone
export OS_AUTH_TENANT="{{ tenant }}"
export OS_AUTH_URL="{{ keystone_url }}"
export OS_AUTH_USER="{{ user }}"
export USE_KEYSTONE=true
