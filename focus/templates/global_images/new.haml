-extends "global_admin_base.haml"
-block amdjs
  {{ super () }}
  %script
    window.kernel_list = {{ kernel_list | safe }};
    window.initrd_list = {{ initrd_list | safe }};
  %script src="{{ url_for('static', filename='amd/main_images.js') }}"
-block content
  -from "_formhelpers.haml" import render_h_field
  %form.form-horizontal.well.new-image method="POST" action="{{ url_for('.create') }}" data-upload-action="{{ url_for('.upload') }}"
    %div.upload-view
