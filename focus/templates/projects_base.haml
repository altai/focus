-extends "authenticated_base.haml"
-block subnav
  %div.container
    %div.subnav
      %ul.nav.nav-pills
        %li class="{% if title == 'Virtual Machines' %}active{% endif %}"
          %a href="{{ url_for('project_views.show_tenant', tenant_id=g.tenant_id) }}"
            Virtual Machines
        %li class="{% if request.path == url_for('project_views.billing', tenant_id=g.tenant_id) %}active{% endif %}"
          %a href="{{ url_for('project_views.billing', tenant_id=g.tenant_id) }}"
            Billing
        %li class="{% if request.path == url_for('project_views.list_users', tenant_id=g.tenant_id) %}active{% endif %}"
          %a href="{{ url_for('project_views.list_users', tenant_id=g.tenant_id) }}"
            Users
        %li class="{% if request.blueprint == 'project_images' %}active{% endif %}"
          %a href="{{ url_for('project_images.index', tenant_id=g.tenant_id) }}"
            Images
        %li class="{% if request.blueprint == 'ssh_keys' %}active{% endif %}"
          %a href="{{ url_for('ssh_keys.index', tenant_id=g.tenant_id) }}"
            SSH Keys                  
        %li class="{% if request.blueprint == 'project_security_groups' %}active{% endif %}"
          %a href="{{ url_for('project_security_groups.index', tenant_id=g.tenant_id) }}"
            Security Groups
        %li class="{% if request.path == url_for('project_views.get_credentials', tenant_id=g.tenant_id) %}active{% endif %}"
          %a href="{{ url_for('project_views.get_credentials') }}"
            Get Credentials File
    %header.jumbotron.subhead#overview
      %h1
        {{ g.tenant.name }}: {{ title }}
      %p.lead
        {{ subtitle }}
